# Interview Intel v5.0 - 快速开始

## ⚡ 一键执行工作流

这是使用 Interview Intel 最快的方式 - 一条命令生成所有面试准备材料。

### 最简单用法

```bash
python scripts/all_in_one.py execute \
  --base-path ~/InterviewIntel \
  --company "公司名称" \
  --role "职位名称" \
  --jd-file jd.txt \
  --resume-version v1.0
```

### 完整示例

```bash
python scripts/all_in_one.py execute \
  --base-path ~/InterviewIntel \
  --company "京东物流" \
  --role "运输产品经理" \
  --jd-file jd_logistics.txt \
  --resume-version v1.0 \
  --resume-file my_resume.txt \
  --achievement "主导 AI 产品从 0 到 1,用户增长 300%" \
  --years 6 \
  --insight "物流供应链智能化"
```

## 📦 输出内容

一条命令生成所有文件，保存到 `companies/公司名称/`:

1. **JD 深度分析** (`jd_analysis_*.md`)
   - 职位定位透视
   - 职责拆解与能力要求
   - 关键技能矩阵
   - JD 潜台词洞察
   - 高频面试问题预测

2. **简历匹配报告** (`resume_mapping_*.md`)
   - 匹配度总览（量化评分）
   - 详细匹配度对照表
   - 能力象限分析
   - 简历优化建议（STAR 改写示例）
   - 面试策略和破冰文案

3. **面试攻防策略** (`interview_strategy_*.md`)
   - 第一轮（HR筛选）：风险识别 + 防御脚本
   - 第二轮（业务负责人）：高频问题 + 杀手案例
   - 第三轮（高管面试）：战略问题 + 商业思维

4. **破冰文案** (`icebreaker_*.md`)
   - 策略 A：实力精准匹配（适合 HR）
   - 策略 B：业务痛点共鸣（适合业务负责人）
   - 使用指南、发送时机、禁忌词汇

5. **原始数据** (`raw_data/`)
   - 原始 JD 文本
   - 关键词提取结果

## 🎯 使用场景

### 场景 1：刚拿到新 JD

```bash
# 1. 保存 JD 到文件
cat > jd_bytedance.txt <<EOF
字节跳动 - 产品经理
[粘贴 JD 内容]
EOF

# 2. 一键生成分析
python scripts/all_in_one.py execute \
  --base-path ~/InterviewIntel \
  --company "字节跳动" \
  --role "产品经理" \
  --jd-file jd_bytedance.txt \
  --resume-version v1.0

# 3. 查看结果
open companies/字节跳动/jd_analysis_产品经理.md
```

### 场景 2：准备面试

```bash
# 生成完整准备包
python scripts/all_in_one.py execute \
  --base-path ~/InterviewIntel \
  --company "腾讯" \
  --role "后端工程师" \
  --jd-file jd_tencent.txt \
  --resume-version v2.0 \
  --resume-file my_backend_resume.txt \
  --achievement "优化系统性能，QPS 提升 5 倍" \
  --years 5

# 重点查看面试策略
open companies/腾讯/interview_strategy_后端工程师.md
```

### 场景 3：准备开场白（Boss直聘/LinkedIn）

```bash
# 生成破冰文案
python scripts/all_in_one.py execute \
  --base-path ~/InterviewIntel \
  --company "美团" \
  --role "技术经理" \
  --jd-file jd_meituan.txt \
  --resume-version v1.0 \
  --achievement "带领团队从 0 到 1 完成核心系统重构" \
  --years 8 \
  --insight "本地生活服务数字化"

# 查看破冰文案
open companies/美团/icebreaker_美团_技术经理.md
```

## 📂 文件组织

所有公司材料自动组织到 `companies/` 目录：

```
InterviewIntel/
├── companies/
│   ├── 字节跳动/
│   │   ├── jd_analysis_产品经理.md
│   │   ├── resume_mapping_产品经理.md
│   │   ├── interview_strategy_产品经理.md
│   │   └── icebreaker_字节跳动_产品经理.md
│   │
│   ├── 腾讯/
│   │   ├── jd_analysis_后端工程师.md
│   │   └── ...
│   │
│   └── 美团/
│       └── ...
│
└── resumes/
    ├── master_resume_v1.0.pdf
    ├── master_resume_v2.0.pdf
    └── resume_registry.json
```

## 🛠️ 可选参数

| 参数 | 说明 | 必需 | 示例 |
|------|------|------|------|
| `--base-path` | 基础路径（包含 companies/ 文件夹） | ✅ | `~/InterviewIntel` |
| `--company` | 公司名称 | ✅ | `"京东物流"` |
| `--role` | 职位名称 | ✅ | `"运输产品经理"` |
| `--jd-file` | JD 文件路径 | ✅ | `jd.txt` |
| `--resume-version` | 简历版本 | ✅ | `v1.0` |
| `--resume-file` | 简历内容文件（用于匹配分析） | ❌ | `resume.txt` |
| `--achievement` | 顶尖成就（用于破冰文案） | ❌ | `"主导产品从 0 到 1"` |
| `--years` | 相关经验年限 | ❌ | `6` |
| `--insight` | 行业洞察（用于破冰策略 B） | ❌ | `"物流智能化"` |

## 💡 下一步

生成完成后：

1. **查看 JD 分析**
   - 了解职位定位和核心要求
   - 识别 JD 潜台词（他们真正想要什么）
   - 预习高频面试问题

2. **查看简历匹配报告**
   - 评估匹配度（量化评分）
   - 找出最大短板
   - 参考 STAR 改写示例优化简历

3. **查看面试攻防策略**
   - HR 轮：准备防御脚本应对风险问题
   - 业务轮：准备杀手案例和深度问题
   - 高管轮：准备战略问题和商业思维

4. **查看破冰文案**
   - 选择策略 A（专业匹配）或策略 B（业务洞察）
   - 根据对方身份（HR vs 业务负责人）选择
   - 参考最佳发送时机和禁忌词汇

## 🔧 高级用法

如果只需要部分功能，可以使用独立脚本：

```bash
# 只生成 JD 分析 + 简历匹配
python scripts/resume_optimizer.py analyze \
  --company-path companies/公司名 \
  --company "公司名" \
  --role "职位" \
  --jd-file jd.txt \
  --resume-version v1.0

# 只生成面试策略
python scripts/interview_strategy.py generate \
  --company-path companies/公司名 \
  --company "公司名" \
  --role "职位" \
  --resume-version v1.0

# 只生成破冰文案
python scripts/icebreaker_generator.py generate \
  --company-path companies/公司名 \
  --company "公司名" \
  --role "职位" \
  --keywords "产品,设计,数据" \
  --achievement "主导产品从 0 到 1"
```

## 🎉 成功示例

实际使用 Interview Intel 生成的完整案例：

- [京东物流运输产品经理分析](companies/京东物流/) - 60% 匹配度，提供详细补课策略
- [MiniMax AI 产品经理分析](companies/MiniMax/) - 85% 匹配度，提供差异化优势建议

查看这些示例了解输出质量和深度。

## 📚 更多信息

- 完整文档：查看 [SKILL.md](SKILL.md)
- STAR 改写指南：查看 [assets/star_rewrite_template.md](assets/star_rewrite_template.md)
- 独立脚本使用：查看 SKILL.md 的 "Resources Reference" 章节
